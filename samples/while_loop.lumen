# While loop with counter
int count = 0;
int total = 0;

while (count < 5) {
    total = total + count;
    print("count:", count, "running total:", total);
    count = count + 1;
}

print("Final total:", total);
